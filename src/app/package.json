{
  "name": "@onecaptain/app",
  "version": "0.0.160",
  "description": "Run OneCaptain locally — one command, no clone needed",
  "license": "SEE LICENSE IN LICENSE",
  "homepage": "https://github.com/agentaler/onecaptain#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agentaler/onecaptain.git",
    "directory": "src/app"
  },
  "bugs": {
    "url": "https://github.com/agentaler/onecaptain/issues"
  },
  "type": "module",
  "bin": {
    "onecaptain-app": "dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "bundled",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm --external commander --external wrangler && node scripts/bundle-cli.mjs",
    "prepack": "pnpm run build && node -e \"const{existsSync}=require('fs');if(!existsSync('bundled/web'))throw new Error('bundled/ missing — run pnpm bundle first')\"",
    "bundle": "bun run scripts/bundle.ts",
    "knip": "knip",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "citty": "^0.2.2",
    "commander": "^14.0.3",
    "playwright-core": "^1.62.1",
    "postal-mime": "^2.7.5",
    "wrangler": "^4.118.0"
  },
  "devDependencies": {
    "@onecaptain/shared": "workspace:*",
    "@types/bun": "^1.3.14",
    "eslint": "^9.39.5",
    "knip": "^6.31.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.66.0",
    "vitest": "^4.1.10"
  }
}
